# 08 — Defect Ledger

Code state 2026-09-04 16:04, statuses refreshed by the lead after the fix pass (17:1x). Severity per master prompt §5: 1 data loss / security / crash / hard lock / cannot start or finish a run · 2 broken state reset, unusable inputs, missing playable-loop systems · 3 mobile layout failures, menu navigation, gameplay-breaking camera/UI · 4 performance / loading instability (also used for verification-gate breakage) · 5 visual coherence, audio polish, accessibility, non-blocking quality.

Devices: D desktop KBM, C controller, P phone/tablet touch, A all. Status: FIXED (verified by a test, gate or evidence), IMPLEMENTED (code landed, verification pending), PARTIAL, OPEN. `App.ts` is cited by method name because the file was being split while this was written.

| ID | Subsystem | Sev | Repro | Evidence | Likely cause | Devices | Fix strategy | Regression risk | Lane | Status | Verification |
|---|---|---|---|---|---|---|---|---|---|---|---|
| TQC-001 | Input | 2 | Quick tap of Interact/Fire sometimes ignored | Early Wave 1 runs (STATE.md) | Edges computed per fixed step | A | Latch edges per frame, consume after the step (`InputManager.ts:20-51,143-146`) | Low | 4 | FIXED | `tests/e2e/smoke.spec.ts` |
| TQC-002 | Render | 3 | Start a run: scene nearly black | Pre-fix screenshots | 0–10 authored intensities vs physical units | A | `POINT_LIGHT_SCALE = 34` (`WorldRenderer.ts:16`) | Low | 1 | FIXED | `evidence/desktop-1080p-10…13-gameplay-*.png` |
| TQC-003 | Level | 2 | Parking route: "Loose rounds" unreachable | Headless walk failed | Pickup inside the booth collider | A | Moved to (72.3, 33.4) (`districtLevel.ts:173`) | Low | 2 | FIXED | `loop.test.ts` parking route |
| TQC-004 | Objectives | 2 | Parking route left the log stale | Headless test | Only the exact next zone advanced | A | Later zone completes earlier steps (`objectives.ts:21-31`) | Low | 2 | FIXED | `loop.test.ts:86-87` |
| TQC-005 | Tooling | 4 | `pnpm typecheck` | TS2345 at `deadZones.test.ts:26` (15:40) | Untyped tuple literal | — | Typed fixture | None | 8 | FIXED | typecheck clean 15:52, 16:02 |
| TQC-006 | Persistence | 2 | Radio → save → pick slot 2 while playing slot 1 | `App.saveToSlot` → `GameSession.save` uses `this.slot` (`GameSession.ts:90-103`) | Slot not passed through | A | `session.save(reason, slot)` or update `session.slot`; toast must match | Low | 6 | FIXED | `App.saveToSlot` sets `session.slot` + `meta.lastSlot` before saving |
| TQC-007 | Input | 2 | Tab/I or M in play; tap Items/Map on the touch HUD | Only `Pause` is consumed in `App.fixedUpdate`; touch buttons render but do nothing | Actions bound, never read | A | Consume `Inventory`/`Map` edges → `openInventory/openMap` | Low | 4 | FIXED | `App.fixedUpdate` consumes Inventory/Map edges → `openInventory/openMap` |
| TQC-008 | Touch | 2 | Phone: no on-screen controls | `TouchShell.syncDevice` creates `TouchHud`, `enableTouch(true)` | — | P | — | Medium | 4/5 | IMPLEMENTED | `touchHud.test.ts` passes; `touch.spec.ts` green run not recorded |
| TQC-009 | Touch | 3 | Options → Touch layout | `TouchLayoutEditorScreen` | — | P | — | Medium | 4 | IMPLEMENTED | `touchProfiles.test.ts`; no editor e2e |
| TQC-010 | Audio | 5 | Cues for hits, death, ending, flashlight, equip, dodge absent | `07_…` §2 | Partial binding (menu cues bound 16:03) | A | Bind remaining events | Low | 7 | FIXED | `GameAudio` binds threatHit, playerDied, ending, flashlight, equip, dodge |
| TQC-011 | PWA | 2 | Deploy a new build while a run is open | `service-worker.js:11` `skipWaiting()` on install → `controllerchange` → `location.reload()` (`ServiceWorkerClient.ts:40-44`); toast never actionable | Template and client disagree | A | Remove install-time `skipWaiting`; keep the toast → `SKIP_WAITING`; never reload mid-run | Medium | 9 | FIXED | `service-worker.js` skips waiting only on first install; client reloads only after accepted update |
| TQC-012 | PWA | 5 | Go offline | `ShellNotices.offline/online` | — | A | — | Low | 5 | IMPLEMENTED | e2e `context.setOffline(true)` (none) |
| TQC-013 | Mobile | 3 | Rotate a handheld to portrait in play | `RotateOverlay` + pause (`TouchShell.ts:48-55`) | — | P | — | Low | 5 | IMPLEMENTED | e2e viewport 390×844 (none) |
| TQC-014 | Performance | 4 | Any device | No measurements recorded (`05_…` §8) | Not measured | A | Frame-stats HUD + recorded runs per tier | — | 5 | OPEN | numbers in `10_RELEASE_GATE.md` |
| TQC-015 | QA | 2 | Phone gate of §0.1 | `tests/e2e/touch.spec.ts` (15:47) | — | P | — | — | 8 | IMPLEMENTED | `pnpm test:e2e --project=phone-landscape` green |
| TQC-016 | PWA | 4 | Load `index.html`, inspect network | `public/icons/icon-192.png`, `icon-512.png`, `icon-maskable-512.png` generated 15:58 (`scripts/make-icons.mjs`) | — | A | — | Low | 9 | FIXED | files present (`file` reports valid PNGs); build not re-run |
| TQC-017 | Accessibility | 5 | Reduced motion On, fire | `shakeEnabled: cameraShake && !reducedMotion()` (`GameSession.ts:131`); backdrop drift too | — | A | — | Low | 4 | FIXED | code; no unit test |
| TQC-018 | Accessibility | 5 | Toggle Colour-safe HUD | ▲ / ▲▲ markers + hatched stamina (`hud.css:187-190`) | — | A | — | Low | 4 | FIXED | code; screenshot pending |
| TQC-019 | Gameplay | 5 | Hard difficulty | `ammoFound` applied (`interactions.ts:86`); Difficulty row on New run (`SlotSelectScreen.ts:52-64`) | — | A | — | Low | 2 | IMPLEMENTED | hard-mode `loop.test` variant (none) |
| TQC-020 | Gameplay | 5 | Pause → Items → Reload / Use | Instant, free, literal `100` (`InventoryScreen.ts:95-116`) | Bypasses `player.ts` timers | A | Route through `tryReload/tryMedkit` | Low | 2 | FIXED | `InventoryScreen` routes through `tryReload`/`tryMedkit`, closes menus, timers run in play |
| TQC-021 | Input | 3 | Real browser, pointer locked, Escape | `pointerlockchange` only clears buttons / drops one move (`KeyboardMouseSource.ts:68-71`); no pause | Missing handler | D | Pause on lock loss with no screen open; keep `KeyP` | Low | 4 | FIXED | `App.onPointerLockChange` pauses when lock is lost with no screen open |
| TQC-022 | Performance | 4 | Quality Auto on a slow device | Tier hint static; `AutoQuality` adapts resolution 0.6–1.0 (tested) | Static tier heuristic | A | Re-evaluate tier from measured frames | Medium | 5 | PARTIAL | `autoQuality.test.ts`; manual |
| TQC-023 | App | 4 | Throw inside a frame | `App.onUncaught` logs only | No error surface | A | `ErrorScreen` after repeated errors | Low | 1 | FIXED | `App.onUncaught`: 3 errors in 10 s → `ErrorScreen`, session ended |
| TQC-024 | UI | 5 | Load with a damaged slot | Row disabled yet hint says "Select to delete it" (`SlotSelectScreen.ts:71`) | Disabled in load mode | A | Keep damaged rows selectable | Low | 3 | FIXED | Load mode disables only empty slots; damaged rows offer delete |
| TQC-025 | UI | 5 | Hover one row, arrow to another | Two highlights | Hover independent of `FocusManager` | D | `mouseenter` → `focusElement` | Low | 3 | FIXED | `Screen` mouseover → `focus.focusElement` |
| TQC-026 | Input | 4 | Xbox / PlayStation / Nintendo pads | `gamepad.spec.ts` emulates one Xbox pad end to end; no hardware, no PS/Nintendo browser run | No manual matrix | C | Matrix in `10_RELEASE_GATE.md` | — | 8 | PARTIAL | e2e green + matrix |
| TQC-027 | Mobile | 5 | ≤ 760 px wide menus | Rows 44 px (`base.css:382`) | Compact override | P | Keep 48 px, reduce padding | Low | 5 | FIXED | `base.css` small-screen rows use `--tqc-hit-min` (48 px) |
| TQC-028 | Docs | 5 | `CANON.md` dependents; `ASSET_LEDGER.md:16` | `CANON.md` still names `ObjectiveScreen.ts`, `ThreatVisual.ts`, `interactables.ts` (actual `MapScreen.ts`, `CharacterRig.ts`, `interactions.ts`); asset ledger now exists but says "PNG variants pending" | Written before the code | — | Fix paths; refresh ledger | None | 1 | FIXED | `CANON.md` dependents corrected; asset ledger icon row updated |
| TQC-029 | Process | 4 | `git status` | 39 entries; no commit since `2e01bfa` | Waves 1–6 uncommitted | — | Commit after `pnpm verify` | — | lead | FIXED | Checkpoint commits `2e01bfa`, `9677a8f` (+ later) on `claude/quiet-collapse-audit-adm7fo` |
| TQC-030 | Tooling | 5 | `playwright.config.ts:25` | `npm run preview` under pnpm | Inconsistent runner | — | `pnpm preview` | None | 9 | FIXED | `playwright.config.ts` webServer runs `pnpm preview` |
| TQC-031 | Persistence | 5 | Finish, Return to menu, Continue | Resumes at plaza; `completed` never saved | No save after the gate | A | Save on `ending` | Low | 6 | FIXED | Ending saves `completed: true`; Continue shows "Run complete"; loading a completed run offers a new run |
| TQC-032 | Code health | 5 | grep | `void this.app` (`BootScreen.ts:51`); unused helpers in `01_…` §6 (`PRIMARY_MIN_TARGET_PX`, `GameSession.baseFov`, …) | Scaffolding leftovers | — | Remove or implement | None | 1 | FIXED | `void this.app`, `PRIMARY_MIN_TARGET_PX`, `GameSession.baseFov` removed |
| TQC-033 | Tooling | 4 | `pnpm lint` | `registry.test.ts:4` import type (15:52) | Value import used as type | — | `import type` | None | 8 | FIXED | lint clean 16:04 |
| TQC-034 | Code health | 5 | `pnpm lint` | `App.ts` exceeded `max-lines` 420 (15:52–16:02) | Composition root grew | — | Split into `layers.ts`, `TouchShell.ts`, `ShellNotices.ts` | Low | 1 | FIXED | lint clean 16:04 |
| TQC-035 | Security policy | 5 | `TouchHud.ts:122` | `innerHTML = touchIconSvg(id)` (constant SVG) | Convenience | P | `createElementNS`/`DOMParser` | None | 6 | FIXED | `touchIcons.ts` builds SVG via `DOMParser` + `importNode`; no `innerHTML` in `src/` |
| TQC-036 | Touch | 5 | Touch joystick | No configurable dead zone / sprint threshold (§8) | Not built | P | Settings rows + `TouchHud` thresholds | Low | 4 | FIXED | Settings `touchDeadZone`, `touchSprintThreshold`, `touchSprintLock` + Options rows; `TouchHud.tuning` |
| TQC-037 | Audio | 5 | Any cue | No audio tests; levels unheard | New code | A | Stub-context tests; listening pass | — | 7 | OPEN | tests + gate |
| TQC-038 | Accessibility | 5 | Threat vocal while an objective shows | Caption replaces the HUD message (`GameAudio.ts:115`, `App.startSession`) | Shared message slot | A | Dedicated subtitle line | Low | 4/7 | FIXED | Dedicated `.tqc-hud__caption` line (`Hud.showCaption`) |
| TQC-039 | Process | 4 | Read `STATE.md` | Updated 15:57 to reflect Waves 3–6/10; references `tests/e2e/debug-aim.spec.ts`, which does not exist | — | — | Keep updating per wave; drop the stale reference | — | lead | FIXED | review |
| TQC-040 | Accessibility | 5 | Options → Accessibility | `holdToInteract` works (`Simulation.ts:56-66`) but has no row; only settable via storage | UI not added | A | `toggleItem` in `OptionsScreen.accessibilityRows` | None | 4 | FIXED | Accessibility tab "Hold to interact" toggle |
| TQC-041 | Code health | 5 | `MenuBackdrop.ts:43` | `{ isDoorOpen, pickupsTaken } as never` passed to `WorldRenderer.update` | Missing narrow interface | — | Give `WorldRenderer.update` a `DoorAndPickupState` interface | Low | 1 | FIXED | `WorldRenderer.update(view: WorldView)`; `MenuBackdrop` passes a typed static view |
| TQC-042 | Security | 5 | Response headers | `public/_headers` has nosniff/referrer/permissions but no CSP | Not written | A | Add a strict `Content-Security-Policy` (`default-src 'self'`; worker/manifest self) | Low | 6 | FIXED | `public/_headers` strict CSP (self; inline styles only) |
| TQC-043 | Input | 2 | Tap Fire twice within one frame interval (low fps) | `debug-shot` trace: second synthetic tap fired no shot; `inputManager.test.ts` | Edge = down now ∧ not down last frame; a tap that begins before the next sample looked like a hold | A | Sources mark fresh presses (`InputFrame.pulse`); `InputManager` treats `pressedNow` as an edge | Low | 4 | FIXED | `tests/unit/input/inputManager.test.ts` (fresh tap), `tests/e2e/loop.spec.ts` shooting beat |
| TQC-049 | Gameplay | 3 | Walk with a half-deflected stick or joystick | `touch.spec.ts` trace: 0.64 deflection moved 1.05 m/s instead of 1.66 m/s | Direction vector kept its deflection length and was multiplied by the deflection-scaled speed (quadratic response) | C, P | Normalise the direction before scaling (`player.ts` handleMovement) | Low | 2 | FIXED | `loop.test.ts` half-deflection case; `touch.spec.ts` |
| TQC-050 | Input | 2 | Any source: move the mouse toward you, push the right stick down, or drag down in the touch look zone | `Simulation.applyLook` did `pitch += dy` while every source fed screen-down-positive Y and `CameraRig` treats positive pitch as looking up: all three sources looked UP on a downward input | No shared sign convention for the Look action; each source wrote `lookDeltaY` directly | K, C, P | One Look action: `InputFrame.addLook` (+x right, +y DOWN), sources apply their own invert, sim subtracts y from pitch; per-source "Invert vertical look" (mouse / controller / touch), settings schema v2 with migration of the old flag | Low (desktop and controller vertical feel becomes the standard, un-inverted one; previously inverted players keep it through the migration) | 4 | FIXED | `tests/unit/input/lookConvention.test.ts` (mouse, pad, touch, sim, migration); `touch.spec.ts` asserts drag-down lowers pitch |
| TQC-051 | Gameplay / camera | 2 | Face east (+X), hold D | Playwright probe: player z 21 → 18.56 (north = screen-left when facing +X); camera z 18.13 < player z (left shoulder), so the raised right arm and gun were hidden behind the torso while aiming | `right = (cos yaw, 0, -sin yaw)` is the negated forward × up for a +Z-forward camera; shared by `player.ts` strafe and `CameraRig` shoulder offset | K, C, P | Flip the right vector in both places (screen-right = (-cos yaw, sin yaw)); headless walk helper updated | Medium (camera moves to the right shoulder; aim ray unaffected; loop/gamepad/touch gates re-run green) | 2 | FIXED | `lookConvention.test.ts` strafe case; `loop.spec.ts`, `gamepad.spec.ts`, `touch.spec.ts`, `weapon.spec.ts` evidence `*-31-weapon-aim.png` |
| TQC-052 | Touch | 3 | Drag near the top edge or beside the pause/items/map buttons on a notched phone | Look zone was a fixed 55 % strip from the top to the bottom edge that ignored safe-area insets and contained the top-centre buttons; no geometric rejection of drags starting on a button | Zone drawn by CSS, not from the layout; no shared pointer registry | P, T | `lookZoneRect` = right half inside the safe insets, positioned by `TouchHud.layout`; `PointerOwners` registry (one pointer per control, claims rejected when owned); drags starting on a visible button hit circle are refused; first-use "drag to look" glyph; optional visible right stick (`touchLookControl`) with radial dead zone and squared response | Low | 6 | FIXED | `touchHud.test.ts` (two-pointer simultaneous move+look every frame, cancel, ownership both ways, hit-reject, hint, right stick); `touch.spec.ts` second test |
| TQC-053 | Touch | 3 | Same preset at 19.5:9, 20:9, 4:3, 16:10 | v1 presets positioned controls as fractions of the whole safe area with sizes relative to the short side, so the 8 px margin and gaps did not hold across aspects; overlap check used a 0 px gap; 48 px minimum; pause/items/map sat in the top-centre band | Layout format not edge-anchored; no build gate | P, T | Edge-anchored coordinates (`x = 1` touches the 8 px margin at any aspect), reference-side clamp for sizes, 56 / 72 px minimums, 12 px gaps, look-zone intrusion and crowding checks (`checkLayout`); rebuilt phone/tablet presets; `vite-plugin-touch-layout-check` fails the build on any violation; profiles v2 (v1 layouts reset) | Low | 6 | FIXED | `touchLayout.test.ts`, `touchProfiles.test.ts`, `pnpm build` gate, `docs/audit/touch/after/*.png` (20 captures) |
| TQC-054 | Render | 3 | Watch the player aim or reload | No held weapon; muzzle flash from a fixed offset in front of the torso | Rig had no hand sockets | all | `WeaponRig` (handgun, medkit, torch; PLACEHOLDER_ART) on hand sockets with raise blend, pitch follow, reload roll and a muzzle socket used by the fire effect; HUD equipped-item silhouette | Low | 8 | FIXED | `weapon.spec.ts` evidence `*-30…33-weapon-*.png` |
| TQC-055 | Level | 3 | Stand by the pharmacy shelf, the counter, the booth or the stairwell door | Medkit, ammo box, notebook and the radio floated at authored heights beside furniture; the radio had no mesh; "papers" sheet and the road-closed sign floated; the barrier arm hung with no post | Positions authored by hand, never grounded | all | `groundLevel`: raycast down from each prop / pickup / loose document / radio spawn, place the bottom on the hit surface, skip when nothing lies within 4 m (`SPAWN`); radio body with lit dial; bag under the flashlight, ledge under the notebook, post under the arm; papers removed; QA overlay spawn-ray draw (F10 / button) | Low | 8 | FIXED | `tests/unit/level/grounding.test.ts` (synthetic + whole district: nothing skipped); loop tests |
| TQC-056 | HUD | 4 | Touch HUD on 4:3 or with reload available | Ammo readout overlapped the pause button (4:3) and the contextual reload button (phone) | Fixed 72 px offset; vertical stack | P, T | `TouchHud` publishes `--tqc-touch-top-cluster`; item block laid out horizontally under touch, secondary line hidden | None | 6 | FIXED | `docs/audit/touch/after/tablet-4x3.png`, `twoThumb-19_5x9.png` |
| TQC-057 | Test harness | 3 | Any e2e spec once boot preloads ~7 MB of assets | `openGame`/`passWarning` acted on the boot screen (which also has a `menu` role) and the run never reached the main menu | Helpers assumed boot was instantaneous | — | Both helpers wait for `screens.top.id ∈ {warning, mainMenu}` before acting | None | 1 | FIXED | `weapon.spec.ts`, `offline.spec.ts` green after the preload landed |
| TQC-058 | Navigation | 2 | Edit any level collider, then start a run | The baked navmesh no longer matched the level; the runtime silently fell back to grid pathing and `nav.spec.ts` failed | Bake and level drift apart with no guard | all | Level signature baked into `nav.district`; runtime refuses a stale mesh with a console warning; `navSignature.test.ts` fails the unit suite until `pnpm assets:build` is re-run | Low | 2 | FIXED | `tests/unit/level/navSignature.test.ts`, `tests/e2e/nav.spec.ts` |
| TQC-059 | Level | 2 | Leave the parking structure by its exit door | A shipping container placed for the blocked-route dressing sat across the exit; `loop.test.ts` ("through the parking structure") failed | Dressing authored without re-running the headless route tests | all | Container moved west of the route (`dressing.ts`); the dressed level re-runs every headless route | None | 1 | FIXED | `tests/unit/sim/loop.test.ts` |
| TQC-060 | Render | 3 | Main menu after the kit dressing landed | The backdrop camera sat inside a building block; with textured, open-backed boxes the menu showed floating window frames | Backdrop anchor chosen when blocks were opaque masses | all | Anchor moved onto Ferry Street looking east toward the wreck (`MenuBackdrop.ts`) | None | 1 | FIXED | `docs/audit/evidence/desktop-1080p-02-main-menu.png` |
| TQC-062 | Deployment / security | 1 | Boot the deployed build (any origin serving `public/_headers`) | Boot stalled on the loading screen forever: the strict CSP (`script-src 'self'`, `worker-src 'self'`) refused WebAssembly compilation (meshopt, recast, Basis), blob workers (KTX2Loader) and the Basis transcoder's embind `new Function` invokers; the KTX2 init promise never settled, so preload never finished and the service worker never registered. The local preview serves no headers, so every local gate stayed green | CSP written before wasm/worker dependencies existed; no gate ran against an origin that serves the headers | all (deployed) | `script-src 'self' 'wasm-unsafe-eval' 'unsafe-eval'`, `worker-src 'self' blob:` (reason documented in `_headers`); every asset load now times out after 45 s so a blocked loader degrades to placeholders instead of blocking boot; `live-boot.spec.ts` + `offline.spec.ts` against the QA alias are part of the deploy routine | Low (eval is only reachable by first-party code; no user content is executed) | 3 | FIXED | `E2E_BASE_URL=https://qa.quiet-collapse.pages.dev … live-boot.spec.ts offline.spec.ts` PASS |
| TQC-061 | Render | 4 | Flashlight on a textured wall; plaza concrete under the gate light | Textured surfaces (albedo ≈ 0.6–0.8 vs the old 0.35 flat colours) blew out under the existing light budget | Light intensities tuned for dark placeholder colours | all | `src/config/lighting.ts` tokens (environment 0.07–0.11, key 0.24, point-light scale 22), flashlight 170 cd, concrete tints, Kenney albedo ×0.72 | None | 2 | FIXED | `docs/audit/evidence/*-1[0-5]-gameplay-*.png` |
| TQC-063 | Input | 2 | Pull a trigger slowly on a pad, or use an Android Chrome pad with a non-standard mapping | Aim/fire flickered near the half-pull point and never registered when the browser reported triggers as axes (`buttons[6].value` stayed 0) | Triggers were read as digital with a single 0.5 threshold and no axis fallback | C | `TriggerReader` (`src/input/triggers.ts`): analog value from `buttons[6|7].value`, axis fallback for `mapping !== 'standard'` (rest value learnt on the first snapshot), 0.35 press / 0.25 release hysteresis | Low | 4 | FIXED | `tests/unit/input/triggers.test.ts`; `gamepad.spec.ts` analog pulls |
| TQC-064 | Input | 3 | Remap a button on a PlayStation pad, then plug in an Xbox pad | One shared controller map: a remap for one family silently changed every other | Bindings v1 stored a single pad map | C | Per-family profiles as data (`src/input/padProfiles.ts`), bindings schema v2 with one profile per family, remap screen tabs per family, generic pads use the Xbox layout with numbered glyphs | Low | 4 | FIXED | `tests/unit/input/padProfiles.test.ts` (v1 migration included) |
| TQC-065 | Touch | 3 | Play on a phone with a pad connected, or on a touch laptop with the keyboard | On-screen controls stayed visible whenever the device could touch, over pad and keyboard play; a finger on the stick kept driving movement while the HUD hid | Visibility was tied to presentation, not to the active source; hide did not release pointers first | P | `shouldShowTouchHud` (`src/app/TouchShell.ts`): follows the active-source policy, locked-to-touch keeps it; 150 ms fade with input refused during the fade; hide releases every pointer owner first | Low | 6 | FIXED | `tests/unit/ui/touchVisibility.test.ts`, `touchHud.test.ts` fade case, `gamepad.spec.ts` (`data-touch-hud=false` under a locked pad) |
| TQC-066 | Render | 3 | Aim in third person and look at the pistol | The barrel pointed along the hand joint's X axis instead of the forearm, so the gun stuck out sideways from the fist; the medkit and torch shared the wrong frame | Hand sockets were guessed Euler angles in `config/character.ts`; the universal skeleton's hand joint runs +Y along the fingers/forearm | A | Socket data moved into the item registry (`{ joint, positionOffset, rotationOffset }`), values measured on the skeleton at the aim/torch poses (barrel onto joint +Y, top onto the back of the hand); QA overlay socket tuner with copy-JSON for on-device refinement | Low | 3 | IMPLEMENTED | Values recorded in `src/game/items/registry.ts`; real-device visual check pending (SwiftShader stills only) |
| TQC-067 | Camera / animation | 3 | Tap aim repeatedly; aim then release while walking | Camera distance, field of view, crosshair and the arm pose each smoothed the aim with their own rate (0.22 s raise, damp 12 and damp 10), so they visibly arrived at different times and the crosshair popped | Four owners of one blend | A | One aim value in the simulation (`weaponRaise`, 120 ms in / 180 ms out, interpolated between steps) drives distance, shoulder, field of view, crosshair opacity and the upper-body layer weight; camera collision solves after the blend | Low | 3 | FIXED | `docs/audit/evidence/desktop-1366-ads-before.webm` vs `-after.webm`; `ads.spec.ts` |
| TQC-068 | AI / navigation | 2 | Shoot or get attacked by an affected with the crowd navmesh active | The body slid during attacks and snapped when a stagger ended; corpses could be steered; the agent kept walking to its goal while the state machine held the body | Crowd position was authoritative in every state except stagger; the agent was removed a frame after death | A | Agent is authoritative only in wander/investigate/chase; every other state pauses it (`setAgentPaused`) and teleports it onto the body each step; death removes the agent in the same call (`threatState.ts`); data-driven stats and hit-react / stagger / knockdown / dead states | Medium (crowd pacing changed) | 2 | FIXED | `tests/unit/sim/enemy.test.ts` (no position delta, agent count, no teleport), `nav.spec.ts` |
